extends ./_layout.pug

block vars
  - var title = 'Forms'

block content
  .grid-fluid
    .row.xs-center
      form.grid-fluid
        .row.search-wrapper
          .col.xs-10
            input.search(placeholder='Search', aria-label='Enter search terms')
          .col.xs-2
            button.search-submit(aria-label='Submit your search') 🔍
